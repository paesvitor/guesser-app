import { Typography } from "@material-ui/core";
import React from "react";

function RoomGameStart() {
  return (
    <div>
      <Typography variant="h1">Aguardando o host iniciar a partida</Typography>
    </div>
  );
}

export default RoomGameStart;
